# 🛠 Руководство по установке инструментов для разработки Telegram бота

## 📚 Что нам нужно и зачем (простыми словами)

### 1. **Node.js и npm** ✅ (скорее всего уже установлено)
**Что это:** Это как "мотор" для нашего проекта. Node.js позволяет запускать JavaScript код на компьютере (не только в браузере).

**Зачем нужно:** 
- Наш проект написан на TypeScript/JavaScript
- npm - это "магазин" где мы берем готовые кусочки кода (библиотеки)
- Без этого ничего не запустится

**Как проверить, установлено ли:**
Открой терминал и напиши:
```bash
node --version
npm --version
```

**Если не установлено:**
- Скачай с сайта: https://nodejs.org/
- Выбери версию LTS (Long Term Support) - это самая стабильная версия
- Установи как обычную программу

---

### 2. **Python** 🐍 (нужен для бота и возможно для LLM)
**Что это:** Другой язык программирования, очень популярный для создания ботов.

**Зачем нужно:**
- Telegram боты часто пишут на Python (библиотека python-telegram-bot очень удобная)
- Если будем использовать LLM API (например, OpenAI, Anthropic), они часто работают через Python
- Многие инструменты для работы с данными используют Python

**Как установить:**
1. Скачай с сайта: https://www.python.org/downloads/
2. При установке **ОБЯЗАТЕЛЬНО** поставь галочку "Add Python to PATH" (это важно!)
3. Установи версию 3.10 или новее

**Как проверить:**
```bash
python --version
# или
python3 --version
```

---

### 3. **Docker** 🐳 (для запуска базы данных и сервисов)
**Что это:** Это как "виртуальная коробка", в которой можно запустить любую программу (базу данных, сервер и т.д.) без установки на компьютер.

**Зачем нужно:**
- Мы будем использовать базу данных (PostgreSQL или MongoDB)
- Вместо установки базы данных на компьютер, мы запустим её в Docker
- Это проще, чище и не засоряет компьютер
- Когда появится сервер, там тоже будет Docker

**Как установить:**
1. Скачай Docker Desktop: https://www.docker.com/products/docker-desktop/
2. Установи как обычную программу
3. Запусти Docker Desktop (он должен работать в фоне)

**Как проверить:**
```bash
docker --version
docker ps
```

**Важно:** Docker Desktop должен быть запущен, иначе команды не будут работать.

---

### 4. **Git** ✅ (скорее всего уже установлено)
**Что это:** Система контроля версий. Это как "история изменений" для кода.

**Зачем нужно:**
- Сохранять все изменения в коде
- Откатываться назад, если что-то сломалось
- Работать в команде
- Отправлять код на GitHub

**Как проверить:**
```bash
git --version
```

**Если не установлено:**
- На macOS обычно уже установлен
- Если нет: https://git-scm.com/downloads

---

### 5. **PostgreSQL** (база данных) - через Docker
**Что это:** База данных - это как большая Excel таблица, где мы храним всю информацию (пользователей, сообщения, данные и т.д.)

**Зачем нужно:**
- Хранить данные пользователей
- Хранить историю сообщений
- Хранить настройки бота
- Когда появится сервер, данные будут там

**Как установить:**
Мы НЕ будем устанавливать PostgreSQL напрямую. Вместо этого используем Docker (см. выше).

**Запуск через Docker:**
```bash
docker run --name telegram-bot-db -e POSTGRES_PASSWORD=mysecretpassword -p 5432:5432 -d postgres
```

Это создаст базу данных в Docker контейнере.

---

### 6. **VS Code или Cursor** ✅ (уже используешь)
**Что это:** Редактор кода - это как Word, но для программирования.

**Зачем нужно:**
- Писать код
- Видеть ошибки
- Удобно работать с файлами

**Расширения, которые стоит установить:**
- ESLint (проверка кода на ошибки)
- Prettier (автоматическое форматирование кода)
- Python (если будем использовать Python)
- Docker (для работы с Docker)

---

## 🎯 Минимальный набор для начала работы

**Обязательно:**
1. ✅ Node.js и npm (для Next.js проекта)
2. ✅ Git (для работы с GitHub)
3. ✅ Cursor/VS Code (редактор)

**Для полноценной работы:**
4. 🐍 Python (для бота)
5. 🐳 Docker (для базы данных)

---

## 📋 Чек-лист установки

Пройдись по этому списку и проверь, что установлено:

- [ ] Node.js установлен (`node --version` работает)
- [ ] npm установлен (`npm --version` работает)
- [ ] Git установлен (`git --version` работает)
- [ ] Python установлен (`python --version` работает)
- [ ] Docker установлен (`docker --version` работает)
- [ ] Docker Desktop запущен

---

## 🚀 Что делать дальше

После установки всех инструментов:

1. **Проверь установку** - выполни все команды проверки выше
2. **Установи зависимости проекта:**
   ```bash
   npm install
   ```
3. **Запусти проект:**
   ```bash
   npm run dev
   ```

---

## ❓ Частые вопросы

**Q: Нужен ли мне Python, если проект на TypeScript?**
A: Да, если мы будем делать Telegram бота на Python. Можно сделать бота и на Node.js, но Python проще для новичков.

**Q: Можно ли без Docker?**
A: Технически да, но тогда нужно устанавливать PostgreSQL напрямую на компьютер, что сложнее. Docker - это проще.

**Q: Что если что-то не устанавливается?**
A: Напиши мне, и мы разберемся вместе. Часто проблема в том, что не установлен PATH или нужны права администратора.

---

## 📝 Примечания

- Все эти инструменты бесплатные
- Установка займет примерно 30-60 минут
- После установки компьютер нужно перезагрузить (иногда)
- Если что-то непонятно - спрашивай!

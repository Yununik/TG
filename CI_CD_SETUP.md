# 🚀 Настройка CI/CD для автоматического деплоя на сервер

## 🎯 Что такое CI/CD (простыми словами)

**CI/CD** - это автоматическая система, которая:
- **CI (Continuous Integration)** - проверяет код на ошибки каждый раз, когда ты отправляешь изменения
- **CD (Continuous Deployment)** - автоматически отправляет код на сервер, если все проверки прошли успешно

**Простыми словами:**
Это как "робот-помощник", который:
1. Проверяет, что код работает правильно
2. Если все хорошо - автоматически отправляет его на сервер
3. Тебе не нужно делать это вручную каждый раз

---

## 📋 Что уже готово

Я уже создал два файла в папке `.github/workflows/`:

1. **`ci.yml`** - автоматическая проверка кода
   - Проверяет типы TypeScript
   - Проверяет код на ошибки (ESLint)
   - Собирает проект
   - Работает СЕЙЧАС (как только настроишь GitHub)

2. **`deploy.yml`** - автоматическая отправка на сервер
   - Собирает проект
   - Отправляет на сервер
   - Работает ТОЛЬКО когда появится сервер

---

## 🔧 Настройка CI (проверка кода) - работает сейчас

### Шаг 1: Убедись, что код отправлен на GitHub
```bash
git push origin main
```

### Шаг 2: Проверь, что файлы на месте
- Открой GitHub репозиторий
- Перейди в раздел "Actions"
- Ты должен увидеть, что проверка запустилась автоматически

### Шаг 3: Настрой секреты (если нужны)
Если в коде используются токены или пароли:

1. Открой репозиторий на GitHub
2. Перейди: Settings → Secrets and variables → Actions
3. Нажми "New repository secret"
4. Добавь секреты:
   - `NEXT_PUBLIC_TELEGRAM_BOT_TOKEN` - токен бота (если нужен для сборки)

**ВАЖНО:** Секреты видны только GitHub, их нельзя прочитать из кода.

---

## 🖥 Настройка CD (деплой на сервер) - когда появится сервер

### Вариант 1: Сервер на VPS (свой сервер)

**Что нужно:**
- VPS сервер (например, DigitalOcean, Hetzner, Timeweb)
- SSH доступ к серверу
- Установленный Node.js на сервере

**Шаги:**

1. **Создай SSH ключ на своем компьютере:**
```bash
ssh-keygen -t ed25519 -C "your_email@example.com"
```

2. **Скопируй публичный ключ на сервер:**
```bash
ssh-copy-id user@your-server-ip
```

3. **Добавь секреты в GitHub:**
   - `SERVER_HOST` - IP адрес сервера (например: 123.45.67.89)
   - `SERVER_USER` - имя пользователя на сервере (например: root)
   - `SERVER_SSH_KEY` - приватный SSH ключ (содержимое файла `~/.ssh/id_ed25519`)

4. **Раскомментируй код в `deploy.yml`:**
   Найди строки с комментарием "Пример для VPS через SSH" и убери комментарии.

---

### Вариант 2: Сервер на Railway

**Что нужно:**
- Аккаунт на Railway (https://railway.app)
- Проект на Railway

**Шаги:**

1. **Создай проект на Railway:**
   - Зарегистрируйся на Railway
   - Создай новый проект
   - Подключи GitHub репозиторий

2. **Получи токен:**
   - Settings → Tokens → New Token
   - Скопируй токен

3. **Добавь секрет в GitHub:**
   - `RAILWAY_TOKEN` - токен из Railway

4. **Раскомментируй код в `deploy.yml`:**
   Найди строки с комментарием "Пример для Railway" и убери комментарии.

---

### Вариант 3: Сервер на Render

**Что нужно:**
- Аккаунт на Render (https://render.com)
- Web Service на Render

**Шаги:**

1. **Создай Web Service на Render:**
   - Подключи GitHub репозиторий
   - Выбери ветку `main`
   - Render автоматически настроит деплой

2. **Настрой переменные окружения:**
   - Environment → Add Environment Variable
   - Добавь все нужные переменные

**Примечание:** Render может работать без GitHub Actions, но можно настроить и через Actions.

---

### Вариант 4: Сервер на Vercel (для Next.js)

**Что нужно:**
- Аккаунт на Vercel (https://vercel.com)
- Проект на Vercel

**Шаги:**

1. **Подключи репозиторий к Vercel:**
   - Import Project → выбери GitHub репозиторий
   - Vercel автоматически настроит деплой

2. **Настрой переменные окружения:**
   - Settings → Environment Variables
   - Добавь все нужные переменные

**Примечание:** Vercel работает автоматически, GitHub Actions может не понадобиться.

---

### Вариант 5: Docker на сервере

**Что нужно:**
- Сервер с Docker
- Docker Registry (Docker Hub, GitHub Container Registry)

**Шаги:**

1. **Создай Dockerfile** (если еще нет):
```dockerfile
FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

2. **Добавь секреты в GitHub:**
   - `DOCKER_USERNAME` - имя пользователя Docker Hub
   - `DOCKER_PASSWORD` - пароль Docker Hub

3. **Раскомментируй код в `deploy.yml`:**
   Найди строки с комментарием "Пример для Docker" и убери комментарии.

---

## 🔍 Как проверить, что все работает

### Проверка CI:
1. Сделай небольшое изменение в коде
2. Сделай коммит и отправь на GitHub
3. Открой раздел "Actions" на GitHub
4. Должна запуститься проверка
5. Если все зеленое ✅ - все работает!

### Проверка CD (когда появится сервер):
1. Сделай изменение в коде
2. Отправь в ветку `main`
3. Должна запуститься проверка
4. После проверки должен запуститься деплой
5. Проверь, что изменения появились на сервере

---

## ⚠️ Важные моменты

### Безопасность:
- **НИКОГДА** не добавляй секреты напрямую в код
- Всегда используй GitHub Secrets
- Не коммить файлы `.env` с реальными данными

### Производительность:
- CI проверки занимают 2-5 минут
- Деплой занимает 3-10 минут (зависит от сервера)
- Можно настроить кэширование для ускорения

### Откат изменений:
- Если что-то сломалось, можно откатить коммит
- GitHub сохраняет историю всех деплоев
- Можно вернуться к любой предыдущей версии

---

## 📝 Чек-лист настройки

### Для CI (сейчас):
- [ ] Код отправлен на GitHub
- [ ] Файл `.github/workflows/ci.yml` на месте
- [ ] Проверка запускается автоматически
- [ ] Все проверки проходят успешно

### Для CD (когда появится сервер):
- [ ] Выбран тип сервера (VPS/Railway/Render/Vercel/Docker)
- [ ] Сервер настроен и работает
- [ ] Добавлены секреты в GitHub
- [ ] Раскомментирован нужный код в `deploy.yml`
- [ ] Тестовый деплой прошел успешно

---

## 🆘 Что делать, если что-то не работает

### Проблема: CI не запускается
**Решение:**
- Проверь, что файл `.github/workflows/ci.yml` находится в правильной папке
- Проверь, что код отправлен на GitHub
- Проверь раздел "Actions" на GitHub

### Проблема: Деплой не работает
**Решение:**
- Проверь секреты в GitHub (правильно ли они названы)
- Проверь логи в разделе "Actions" (там будет ошибка)
- Проверь доступ к серверу (SSH ключи, токены)

### Проблема: Код не обновляется на сервере
**Решение:**
- Проверь, что деплой запустился
- Проверь логи на сервере
- Проверь, что сервер перезапустился после деплоя

---

## 📚 Полезные ссылки

- [GitHub Actions документация](https://docs.github.com/en/actions)
- [Railway документация](https://docs.railway.app)
- [Render документация](https://render.com/docs)
- [Vercel документация](https://vercel.com/docs)

---

## 🎯 Итог

**Сейчас:**
- CI уже настроен и готов к работе
- Просто отправь код на GitHub, и проверка запустится автоматически

**Когда появится сервер:**
- Выбери тип сервера
- Следуй инструкциям выше
- Настрой деплой за 10-15 минут

**Главное:**
- Все автоматически
- Не нужно делать ничего вручную
- Код всегда проверяется перед деплоем
- Можно откатить изменения, если что-то сломалось

---

## ❓ Вопросы?

Если что-то непонятно - спрашивай! Я помогу настроить все шаг за шагом.

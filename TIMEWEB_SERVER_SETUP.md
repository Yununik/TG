# 🖥 Пошаговая инструкция: Настройка сервера на Timeweb

## 🎯 Что мы будем делать (простыми словами)

Мы создадим виртуальный сервер (VPS) на Timeweb, установим на него все необходимые программы и настроим автоматическую отправку кода с GitHub на сервер.

**Простыми словами:** Создадим "компьютер в интернете", где будет работать наш бот 24/7.

---

## 📋 Шаг 1: Создать VPS на Timeweb

### Что такое VPS?
VPS (Virtual Private Server) - это виртуальный сервер. Это как отдельный компьютер в интернете, который работает постоянно.

### Как создать:

1. **Зайди на сайт Timeweb:**
   - Открой https://timeweb.com
   - Зарегистрируйся или войди в аккаунт

2. **Перейди в раздел "VPS":**
   - В меню найди "VPS" или "Виртуальные серверы"
   - Нажми "Создать VPS" или "Заказать"

3. **Выбери тариф:**
   - Для начала подойдет самый дешевый тариф (обычно от 200-300 рублей/месяц)
   - Минимум: 1-2 GB RAM, 1 CPU, 20 GB диска
   - Операционная система: **Ubuntu 22.04 LTS** (рекомендую)

4. **Настрой сервер:**
   - Выбери регион (ближайший к тебе)
   - Придумай имя сервера (например: "telegram-bot-server")
   - Выбери способ оплаты

5. **Создай сервер:**
   - Нажми "Создать" или "Заказать"
   - Подожди 5-10 минут, пока сервер создается

6. **Получи данные для доступа:**
   - После создания сервера Timeweb покажет:
     - **IP адрес** (например: 123.45.67.89)
     - **Логин** (обычно: root)
     - **Пароль** (сохрани его!)
   - **ВАЖНО:** Сохрани эти данные в безопасном месте!

---

## 📋 Шаг 2: Подключиться к серверу через SSH

### Что такое SSH?
SSH - это способ подключиться к серверу через терминал. Это как "удаленный доступ" к серверу.

### Как подключиться:

1. **Открой терминал на своем Mac**

2. **Выполни команду:**
```bash
ssh root@ТВОЙ_IP_АДРЕС
```
Замени `ТВОЙ_IP_АДРЕС` на IP адрес, который дал Timeweb.

**Пример:**
```bash
ssh root@123.45.67.89
```

3. **При первом подключении:**
   - Система спросит: "Are you sure you want to continue connecting?"
   - Напиши: `yes` и нажми Enter

4. **Введи пароль:**
   - Введи пароль, который дал Timeweb
   - **ВАЖНО:** При вводе пароля символы не отображаются (это нормально!)
   - Нажми Enter

5. **Если подключился успешно:**
   - Ты увидишь приглашение типа: `root@server:~#`
   - Это значит, что ты на сервере!

---

## 📋 Шаг 3: Обновить систему на сервере

### Зачем это нужно?
Чтобы установить последние обновления безопасности и исправления.

### Команды (выполни по очереди):

```bash
# Обновить список пакетов
apt update

# Установить обновления
apt upgrade -y

# Перезагрузить сервер (если нужно)
reboot
```

**После перезагрузки подключись снова через SSH.**

---

## 📋 Шаг 4: Установить необходимые программы

### Что нужно установить:
1. **Node.js** - для запуска Next.js проекта
2. **Python** - для Telegram бота
3. **Docker** - для базы данных
4. **Git** - для получения кода с GitHub
5. **Nginx** - для веб-сервера (опционально)

### Команды для установки:

#### 1. Установить Node.js:
```bash
# Установить Node.js через nvm (рекомендуемый способ)
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash

# Перезагрузить терминал или выполнить:
source ~/.bashrc

# Установить Node.js версии 20
nvm install 20
nvm use 20
nvm alias default 20

# Проверить установку
node --version
npm --version
```

#### 2. Установить Python:
```bash
# Python обычно уже установлен, но проверим
python3 --version

# Если не установлен:
apt install python3 python3-pip -y
```

#### 3. Установить Docker:
```bash
# Установить Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh

# Запустить Docker
systemctl start docker
systemctl enable docker

# Проверить установку
docker --version
```

#### 4. Установить Git:
```bash
# Git обычно уже установлен, но проверим
git --version

# Если не установлен:
apt install git -y
```

#### 5. Установить Nginx (опционально, для веб-сервера):
```bash
apt install nginx -y
systemctl start nginx
systemctl enable nginx
```

---

## 📋 Шаг 5: Настроить базу данных на сервере

### Запустить PostgreSQL в Docker:

```bash
# Запустить базу данных PostgreSQL
docker run --name telegram-bot-db \
  -e POSTGRES_PASSWORD=mysecretpassword \
  -p 5432:5432 \
  -d \
  --restart unless-stopped \
  postgres

# Создать базу данных
docker exec -it telegram-bot-db psql -U postgres -c "CREATE DATABASE telegram_bot;"
```

**Что делает команда:**
- `--restart unless-stopped` - база данных будет автоматически запускаться при перезагрузке сервера

---

## 📋 Шаг 6: Настроить SSH ключи для GitHub Actions

### Зачем это нужно?
Чтобы GitHub Actions мог автоматически отправлять код на сервер.

### Как настроить:

1. **Создать SSH ключ на сервере:**
```bash
# Создать папку для ключей
mkdir -p ~/.ssh
chmod 700 ~/.ssh

# Создать SSH ключ
ssh-keygen -t ed25519 -C "github-actions" -f ~/.ssh/github_actions -N ""

# Показать публичный ключ (скопируй его)
cat ~/.ssh/github_actions.pub
```

2. **Добавить публичный ключ на сервер:**
```bash
# Добавить ключ в authorized_keys
cat ~/.ssh/github_actions.pub >> ~/.ssh/authorized_keys
chmod 600 ~/.ssh/authorized_keys
```

3. **Скопировать приватный ключ:**
```bash
# Показать приватный ключ (скопируй его полностью)
cat ~/.ssh/github_actions
```

**ВАЖНО:** Сохрани приватный ключ в безопасном месте! Он понадобится для настройки GitHub Secrets.

---

## 📋 Шаг 7: Настроить GitHub Secrets

### Что такое GitHub Secrets?
Это безопасное хранилище для паролей и ключей. GitHub Actions может использовать их, но никто не увидит их содержимое.

### Как настроить:

1. **Открой свой репозиторий на GitHub**

2. **Перейди в настройки:**
   - Settings → Secrets and variables → Actions

3. **Добавь секреты (New repository secret):**

   **Секрет 1: SERVER_HOST**
   - Name: `SERVER_HOST`
   - Value: IP адрес твоего сервера (например: 123.45.67.89)

   **Секрет 2: SERVER_USER**
   - Name: `SERVER_USER`
   - Value: `root`

   **Секрет 3: SERVER_SSH_KEY**
   - Name: `SERVER_SSH_KEY`
   - Value: Приватный SSH ключ (который ты скопировал на шаге 6)

   **Секрет 4: DATABASE_PASSWORD**
   - Name: `DATABASE_PASSWORD`
   - Value: `mysecretpassword` (или другой пароль, если изменил)

---

## 📋 Шаг 8: Настроить автоматический деплой

### Обновить файл deploy.yml:

Я обновлю файл `.github/workflows/deploy.yml` для работы с твоим сервером.

**Что будет делать:**
- Автоматически собирать проект
- Отправлять код на сервер через SSH
- Запускать бота на сервере

---

## 📋 Шаг 9: Создать папку для проекта на сервере

### Команды:

```bash
# Создать папку для проекта
mkdir -p /var/www/telegram-bot
cd /var/www/telegram-bot

# Установить права доступа
chown -R $USER:$USER /var/www/telegram-bot
```

---

## ✅ Проверка: Все ли готово?

### Чек-лист:

- [ ] VPS создан на Timeweb
- [ ] Подключился к серверу через SSH
- [ ] Обновил систему
- [ ] Установил Node.js (проверь: `node --version`)
- [ ] Установил Python (проверь: `python3 --version`)
- [ ] Установил Docker (проверь: `docker --version`)
- [ ] Установил Git (проверь: `git --version`)
- [ ] Запустил базу данных PostgreSQL
- [ ] Создал базу данных `telegram_bot`
- [ ] Создал SSH ключи
- [ ] Добавил секреты в GitHub
- [ ] Создал папку для проекта на сервере

---

## 🆘 Что делать, если что-то не работает?

### Проблема: Не могу подключиться через SSH
**Решение:**
- Проверь IP адрес
- Проверь пароль
- Убедись, что сервер запущен в панели Timeweb

### Проблема: Команды не выполняются
**Решение:**
- Убедись, что ты подключен к серверу (видишь `root@server:~#`)
- Проверь, что используешь правильные команды для Ubuntu

### Проблема: Docker не запускается
**Решение:**
- Выполни: `systemctl status docker`
- Если ошибка - напиши мне, разберемся

---

## 📝 Что дальше?

После выполнения всех шагов:
1. Напиши мне, что все готово
2. Я обновлю файл `deploy.yml` для автоматического деплоя
3. Мы протестируем отправку кода на сервер
4. Начнем создавать бота!

---

## 🎯 Итог

После выполнения всех шагов у тебя будет:
- ✅ Рабочий сервер на Timeweb
- ✅ Все необходимые программы установлены
- ✅ База данных запущена
- ✅ Настроен автоматический деплой с GitHub

**Готов начинать? Начни с Шага 1!** 🚀

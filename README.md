# Telegram Mini App - Автоматизация Юридического Документооборота

Telegram Mini App для автоматизации создания юридических документов (договоров) с возможностью управления профилем, парсинга реквизитов контрагента и генерации PDF с подписью.

## 🚀 Быстрый Старт

### Установка зависимостей

```bash
npm install
# или
pnpm install
# или
yarn install
```

### Запуск в режиме разработки

```bash
npm run dev
```

Приложение будет доступно по адресу [http://localhost:3000](http://localhost:3000)

### Настройка туннеля для Telegram

Для локальной разработки используйте ngrok или cloudflared:

```bash
# ngrok
ngrok http 3000

# cloudflared
cloudflared tunnel --url http://localhost:3000
```

Полученный HTTPS URL используйте в BotFather для настройки Mini App.

## 📁 Структура Проекта

```
TG/
├── app/                    # Next.js App Router
│   ├── layout.tsx         # Корневой layout
│   ├── page.tsx           # Главная страница
│   ├── profile/           # Страница профиля
│   ├── contract/          # Конструктор договоров
│   └── api/               # API routes (если нужны)
├── components/            # React компоненты
│   ├── ui/               # Shadcn/UI компоненты
│   ├── profile/          # Компоненты профиля
│   ├── contract/         # Компоненты конструктора
│   └── pdf/              # PDF компоненты
├── lib/                  # Утилиты и хелперы
│   ├── db/               # IndexedDB (Dexie)
│   ├── telegram/         # Telegram SDK утилиты
│   ├── pdf/              # PDF генерация
│   └── parser/           # Парсер реквизитов
├── hooks/                # Custom React hooks
├── store/                # Zustand store
├── types/                # TypeScript типы
└── public/               # Статические файлы
```

## 🛠 Технологический Стек

- **Framework:** Next.js 14+ (App Router)
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **UI Components:** Shadcn/UI
- **Icons:** Lucide React
- **PDF Generation:** pdf-lib
- **State Management:** Zustand
- **Form Handling:** React Hook Form + Zod
- **Storage:** IndexedDB (Dexie.js)
- **Telegram SDK:** @twa-dev/sdk

## 📚 Документация

Подробное техническое задание и архитектурный план находятся в файле [TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md)

## 🔧 Настройка

1. Скопируйте `.env.example` в `.env.local` и заполните переменные окружения
2. Настройте бота в BotFather
3. Укажите URL вашего приложения в настройках Mini App

## 📝 Roadmap

См. раздел "Step-by-Step Roadmap" в [TECHNICAL_SPECIFICATION.md](./TECHNICAL_SPECIFICATION.md)

## 📄 Лицензия

Private project

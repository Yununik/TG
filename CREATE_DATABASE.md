# 📝 Инструкция: Создать базу данных и настройки

## Шаг 1: Создать базу данных внутри PostgreSQL

Выполни в терминале эту команду:

```bash
docker exec -it telegram-bot-db psql -U postgres -c "CREATE DATABASE telegram_bot;"
```

**Что она делает:**
- Подключается к PostgreSQL внутри контейнера
- Создает базу данных с именем `telegram_bot`

**Если увидишь "CREATE DATABASE"** - все хорошо! ✅

**Если увидишь "already exists"** - база уже создана, это нормально! ✅

---

## Шаг 2: Создать файл с настройками

Создай файл `.env.local` в корне проекта (там же, где `package.json`).

### Способ 1: Через терминал
```bash
cd "/Users/yunussov/Desktop/TG BOT/TG"
cat > .env.local << 'EOF'
# Настройки базы данных PostgreSQL
DATABASE_URL=postgresql://postgres:mysecretpassword@localhost:5432/telegram_bot
POSTGRES_USER=postgres
POSTGRES_PASSWORD=mysecretpassword
POSTGRES_DB=telegram_bot
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Настройки Telegram бота (заполни позже)
# NEXT_PUBLIC_TELEGRAM_BOT_TOKEN=твой_токен_бота
# TELEGRAM_BOT_TOKEN=твой_токен_бота

# Настройки для LLM API (если будешь использовать)
# OPENAI_API_KEY=твой_ключ_openai
# ANTHROPIC_API_KEY=твой_ключ_anthropic
EOF
```

### Способ 2: Вручную
1. Открой Cursor
2. Создай новый файл `.env.local` в корне проекта
3. Скопируй туда содержимое из способа 1

---

## Шаг 3: Проверить, что все работает

Выполни в терминале:

```bash
docker exec -it telegram-bot-db psql -U postgres -d telegram_bot -c "SELECT version();"
```

**Если увидишь версию PostgreSQL** - все работает! ✅

---

## ✅ Готово!

Теперь у тебя:
- ✅ База данных PostgreSQL запущена в Docker
- ✅ База данных `telegram_bot` создана
- ✅ Файл с настройками подключения создан

**Можно начинать создавать бота!** 🚀
